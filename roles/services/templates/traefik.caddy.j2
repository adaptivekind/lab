https://traefik.{{ lab_domain }} {
  reverse_proxy {{ groups['server'][0] }}:443 {
    transport http {
      tls_server_name traefik.local
      tls_trusted_ca_certs /etc/caddy/certs/cluster-ca.crt
    }
  }
  redir / /dashboard/ 302
}

